// Compatibility shim for the extracted PlaneGCS target.
#pragma once

// PlaneGCS includes FCConfig.h for the full FreeCAD build environment, but the
// extracted solver does not consume any configuration macros in this build.
