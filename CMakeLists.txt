cmake_minimum_required(VERSION 3.25)
project(Vertex VERSION 0.1.0 LANGUAGES C CXX)

set(CMAKE_CXX_STANDARD 20)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
set(CMAKE_CXX_EXTENSIONS OFF)
set(CMAKE_EXPORT_COMPILE_COMMANDS ON)
include(CTest)

set(SKETCH_DEPS_DIR "${CMAKE_SOURCE_DIR}/.deps/src" CACHE PATH "Verified local dependency source cache")
option(SKETCH_BUILD_DESKTOP "Build the native desktop application" OFF)
option(SKETCH_BUILD_ARCHITECTURE "Build the Open CASCADE architectural solid engine" OFF)

set(SQLITE_SOURCE_DIR "${SKETCH_DEPS_DIR}/sqlite/sqlite-amalgamation-3530400")
if(NOT EXISTS "${SQLITE_SOURCE_DIR}/sqlite3.c" OR NOT EXISTS "${SKETCH_DEPS_DIR}/json/include/nlohmann/json.hpp")
  message(FATAL_ERROR "Local dependencies are missing. Run python scripts/bootstrap.py (or --offline with cached archives).")
endif()

if(SKETCH_BUILD_ARCHITECTURE OR SKETCH_BUILD_DESKTOP)
  set(SKETCH_OCCT_COMPONENTS FoundationClasses ModelingData ModelingAlgorithms)
  if(SKETCH_BUILD_DESKTOP)
    list(APPEND SKETCH_OCCT_COMPONENTS Visualization)
  endif()
  get_directory_property(SKETCH_PRE_OCCT_DEFINITIONS COMPILE_DEFINITIONS)
  find_package(OpenCASCADE CONFIG REQUIRED COMPONENTS ${SKETCH_OCCT_COMPONENTS})
  find_package(PROJ CONFIG REQUIRED)
  get_directory_property(SKETCH_OCCT_DEFINITIONS COMPILE_DEFINITIONS)
  # The SDK appends its own macros at directory scope. Keep those on the CAD
  # boundary instead of changing unrelated storage and calculation compilation.
  set_directory_properties(PROPERTIES COMPILE_DEFINITIONS "${SKETCH_PRE_OCCT_DEFINITIONS}")
  add_library(sketch_architecture src/architecture/architecture.cpp)
  target_include_directories(sketch_architecture PUBLIC include)
  target_include_directories(sketch_architecture SYSTEM PUBLIC "${OpenCASCADE_INCLUDE_DIR}")
  target_compile_definitions(sketch_architecture PUBLIC ${SKETCH_OCCT_DEFINITIONS})
  target_link_libraries(sketch_architecture PUBLIC sketch_wall_semantics sketch_slab_semantics sketch_terrain_surface sketch_roof_join_semantics sketch_opening_assembly sketch_door_operation TKernel TKMath TKG2d TKG3d TKGeomBase TKBRep TKGeomAlgo TKTopAlgo TKPrim TKBO TKBool)
  add_library(sketch_building_objects src/architecture/building_objects.cpp)
  target_link_libraries(sketch_building_objects PUBLIC sketch_architecture)
  add_library(sketch_building_entities src/architecture/building_entity.cpp src/architecture/document_solid.cpp)
  target_link_libraries(sketch_building_entities PUBLIC sketch_building_objects sketch_document)
  add_library(sketch_performance_workload src/core/performance_workload.cpp)
  target_include_directories(sketch_performance_workload PUBLIC include)
  target_link_libraries(sketch_performance_workload PUBLIC sketch_building_entities sketch_document TKMesh)
  add_executable(performance-workload src/core/performance_workload_main.cpp)
  target_link_libraries(performance-workload PRIVATE sketch_performance_workload sketch_storage)
  add_library(sketch_architectural_schedule src/architecture/architectural_schedule.cpp)
  target_link_libraries(sketch_architectural_schedule PUBLIC sketch_building_entities sketch_document_schedule_adapter)
  if(MSVC)
    target_compile_options(sketch_architectural_schedule PRIVATE /W4 /permissive- /utf-8)
  endif()
  add_library(sketch_building_plan_projection src/architecture/building_plan_projection.cpp
    src/architecture/building_view_projection.cpp)
  target_link_libraries(sketch_building_plan_projection PUBLIC sketch_building_entities TKHLR TKBool)
  add_library(sketch_calculations src/calculations/calculations.cpp)
  target_include_directories(sketch_calculations PUBLIC include)
  target_link_libraries(sketch_calculations PUBLIC sketch_architecture)
  if(MSVC)
    target_compile_options(sketch_architecture PRIVATE /W4 /permissive- /utf-8)
    target_compile_options(sketch_building_objects PRIVATE /W4 /permissive- /utf-8)
    target_compile_options(sketch_building_entities PRIVATE /W4 /permissive- /utf-8)
    target_compile_options(sketch_performance_workload PRIVATE /W4 /permissive- /utf-8)
    target_compile_options(performance-workload PRIVATE /W4 /permissive- /utf-8)
    target_compile_options(sketch_building_plan_projection PRIVATE /W4 /permissive- /utf-8)
    target_compile_options(sketch_calculations PRIVATE /W4 /permissive- /utf-8)
  endif()
  if(BUILD_TESTING)
    add_executable(architectural_schedule_tests tests/architectural_schedule_tests.cpp)
    target_link_libraries(architectural_schedule_tests PRIVATE sketch_architectural_schedule)
    add_test(NAME architectural_schedule COMMAND architectural_schedule_tests)
    add_executable(architecture_tests tests/architecture_tests.cpp)
    target_link_libraries(architecture_tests PRIVATE sketch_architecture)
    add_test(NAME architecture COMMAND architecture_tests)
    add_executable(wall_join_tests tests/wall_join_tests.cpp)
    target_link_libraries(wall_join_tests PRIVATE sketch_architecture sketch_document)
    add_test(NAME wall_join COMMAND wall_join_tests)
    add_executable(roof_join_tests tests/roof_join_tests.cpp)
    target_link_libraries(roof_join_tests PRIVATE sketch_architecture sketch_document sketch_building_entities)
    add_test(NAME roof_join COMMAND roof_join_tests)
    add_executable(opening_assembly_tests tests/opening_assembly_tests.cpp)
    target_link_libraries(opening_assembly_tests PRIVATE sketch_architecture sketch_document)
    add_test(NAME opening_assembly COMMAND opening_assembly_tests)
    add_executable(building_object_tests tests/building_object_tests.cpp)
    target_link_libraries(building_object_tests PRIVATE sketch_building_objects)
    add_test(NAME building_objects COMMAND building_object_tests)
    add_executable(building_entity_tests tests/building_entity_tests.cpp)
    target_link_libraries(building_entity_tests PRIVATE sketch_building_entities)
    add_test(NAME building_entities COMMAND building_entity_tests)
    add_executable(building_plan_projection_tests tests/building_plan_projection_tests.cpp)
    target_link_libraries(building_plan_projection_tests PRIVATE sketch_building_plan_projection)
    add_test(NAME building_plan_projection COMMAND building_plan_projection_tests)
    add_executable(building_view_projection_tests tests/building_view_projection_tests.cpp)
    target_link_libraries(building_view_projection_tests PRIVATE sketch_building_plan_projection)
    add_test(NAME building_view_projection COMMAND building_view_projection_tests)
    add_executable(calculation_tests tests/calculation_tests.cpp)
    target_link_libraries(calculation_tests PRIVATE sketch_calculations)
    add_test(NAME calculations COMMAND calculation_tests)
    add_executable(terrain_surface_tests tests/terrain_surface_tests.cpp)
    target_link_libraries(terrain_surface_tests PRIVATE sketch_terrain_surface)
    add_test(NAME terrain_surface COMMAND terrain_surface_tests)
    set_tests_properties(architectural_schedule architecture wall_join roof_join opening_assembly building_objects building_entities building_plan_projection building_view_projection calculations terrain_surface PROPERTIES ENVIRONMENT_MODIFICATION
      "PATH=path_list_prepend:${CMAKE_SOURCE_DIR}/.deps/native/x64-windows/$<IF:$<CONFIG:Debug>,debug/bin,bin>")
  endif()
endif()
add_library(sketch_sqlite STATIC "${SQLITE_SOURCE_DIR}/sqlite3.c")
target_include_directories(sketch_sqlite PUBLIC "${SQLITE_SOURCE_DIR}")
target_compile_definitions(sketch_sqlite PRIVATE SQLITE_THREADSAFE=1 SQLITE_OMIT_LOAD_EXTENSION SQLITE_DQS=0 SQLITE_DEFAULT_MEMSTATUS=0)
add_library(sketch_json INTERFACE)
target_include_directories(sketch_json SYSTEM INTERFACE "${SKETCH_DEPS_DIR}/json/include")
add_library(sketch_project_resource_catalog src/core/project_resource_catalog.cpp)
target_include_directories(sketch_project_resource_catalog PUBLIC include)
target_link_libraries(sketch_project_resource_catalog PUBLIC sketch_json bcrypt)
if(MSVC)
  target_compile_options(sketch_project_resource_catalog PRIVATE /W4 /permissive- /utf-8)
endif()
if(BUILD_TESTING)
  add_executable(project_resource_catalog_tests tests/project_resource_catalog_tests.cpp)
  target_link_libraries(project_resource_catalog_tests PRIVATE sketch_project_resource_catalog)
  add_test(NAME project_resource_catalog COMMAND project_resource_catalog_tests)
endif()
add_library(sketch_annotation_catalog src/core/annotation_catalog.cpp)
target_include_directories(sketch_annotation_catalog PUBLIC include)
target_link_libraries(sketch_annotation_catalog PUBLIC sketch_json)
add_library(sketch_sheet_view_model src/core/sheet_view_model.cpp)
target_include_directories(sketch_sheet_view_model PUBLIC include)
target_link_libraries(sketch_sheet_view_model PUBLIC sketch_json)
add_library(sketch_interchange_profile src/core/interchange_profile.cpp)
target_include_directories(sketch_interchange_profile PUBLIC include)
target_link_libraries(sketch_interchange_profile PUBLIC sketch_json)
add_library(sketch_dxf_exchange src/core/dxf_exchange.cpp)
target_include_directories(sketch_dxf_exchange PUBLIC include)
if(MSVC)
  target_compile_options(sketch_dxf_exchange PRIVATE /W4 /permissive- /utf-8)
endif()
add_library(sketch_dxf_project_exchange src/core/dxf_project_exchange.cpp)
target_include_directories(sketch_dxf_project_exchange PUBLIC include)
target_link_libraries(sketch_dxf_project_exchange PUBLIC sketch_dxf_exchange sketch_document sketch_precision)
if(MSVC)
  target_compile_options(sketch_dxf_project_exchange PRIVATE /W4 /permissive- /utf-8)
endif()
add_library(sketch_ifc_project_exchange src/core/ifc_project_exchange.cpp)
target_include_directories(sketch_ifc_project_exchange PUBLIC include)
target_link_libraries(sketch_ifc_project_exchange PUBLIC sketch_document sketch_precision)
if(MSVC)
  target_compile_options(sketch_ifc_project_exchange PRIVATE /W4 /permissive- /utf-8)
endif()
add_library(sketch_field_adapter_contract src/core/field_adapter_contract.cpp)
target_include_directories(sketch_field_adapter_contract PUBLIC include)
target_link_libraries(sketch_field_adapter_contract PUBLIC sketch_integration_adapter sketch_json)
add_library(sketch_assistance_contract src/core/assistance_contract.cpp)
target_include_directories(sketch_assistance_contract PUBLIC include)
target_link_libraries(sketch_assistance_contract PUBLIC sketch_json)
add_library(sketch_multipage_document src/core/multipage_document.cpp)
target_include_directories(sketch_multipage_document PUBLIC include)
target_link_libraries(sketch_multipage_document PUBLIC sketch_json)
add_library(sketch_assembly_model src/core/assembly_model.cpp)
target_include_directories(sketch_assembly_model PUBLIC include)
target_link_libraries(sketch_assembly_model PUBLIC sketch_json)
add_library(sketch_product_scope src/core/product_scope.cpp)
target_include_directories(sketch_product_scope PUBLIC include)
target_link_libraries(sketch_product_scope PUBLIC sketch_json)
add_library(sketch_workspace_accessibility src/core/workspace_accessibility.cpp)
target_include_directories(sketch_workspace_accessibility PUBLIC include)
target_link_libraries(sketch_workspace_accessibility PUBLIC sketch_json)
add_library(sketch_survey_contract src/core/survey_contract.cpp)
target_include_directories(sketch_survey_contract PUBLIC include)
target_link_libraries(sketch_survey_contract PUBLIC sketch_json)
add_library(sketch_georeferencing_contract src/core/georeferencing_contract.cpp)
target_include_directories(sketch_georeferencing_contract PUBLIC include)
target_link_libraries(sketch_georeferencing_contract PUBLIC sketch_json)
add_library(sketch_architectural_workflow_contract src/core/architectural_workflow_contract.cpp)
target_include_directories(sketch_architectural_workflow_contract PUBLIC include)
target_link_libraries(sketch_architectural_workflow_contract PUBLIC sketch_json)
add_library(sketch_architectural_document_adapter src/core/architectural_document_adapter.cpp)
target_include_directories(sketch_architectural_document_adapter PUBLIC include)
target_link_libraries(sketch_architectural_document_adapter PUBLIC sketch_architectural_workflow_contract sketch_document sketch_building_entities sketch_architecture)

add_library(sketch_document src/core/document.cpp src/core/document_digest.cpp src/core/constraint_entity.cpp
  src/core/constraint_integrity.cpp src/core/boundary_entity.cpp src/core/boundary_integrity.cpp
  src/core/boundary_dimension.cpp src/core/sheet_view_entity_codec.cpp
  src/core/annotation_entity_codec.cpp src/core/georeferencing_entity_codec.cpp
  src/core/vertical_level_document_adapter.cpp)
target_include_directories(sketch_document PUBLIC include)
target_link_libraries(sketch_document PUBLIC sketch_json PRIVATE sketch_wall_semantics sketch_roof_join_semantics sketch_boundary_receipts
  sketch_slab_semantics sketch_sheet_view_model sketch_annotation_catalog sketch_assembly_model sketch_model_phases
  sketch_room_relationships sketch_vertical_levels sketch_reference_grid sketch_terrain_surface
  sketch_georeferencing_contract bcrypt)
if(SKETCH_BUILD_ARCHITECTURE OR SKETCH_BUILD_DESKTOP)
  add_library(sketch_georeferencing_runtime src/core/georeferencing_runtime.cpp)
  target_include_directories(sketch_georeferencing_runtime PUBLIC include)
  target_link_libraries(sketch_georeferencing_runtime PUBLIC
    sketch_georeferencing_contract sketch_document PROJ::proj)
endif()
add_library(sketch_project_organization src/core/project_organization.cpp)
target_link_libraries(sketch_project_organization PUBLIC sketch_document)
add_library(sketch_project_visibility src/core/project_visibility.cpp)
target_link_libraries(sketch_project_visibility PUBLIC sketch_project_organization
  PRIVATE sketch_wall_semantics sketch_roof_join_semantics)
add_library(sketch_output_fingerprint src/core/output_fingerprint.cpp)
target_link_libraries(sketch_output_fingerprint PUBLIC sketch_document)
add_library(sketch_sheet_output_scene src/core/sheet_output_scene.cpp)
target_link_libraries(sketch_sheet_output_scene PUBLIC sketch_output_fingerprint sketch_sheet_view_model)
if(MSVC)
  target_compile_options(sketch_sheet_output_scene PRIVATE /W4 /permissive- /utf-8)
endif()
if(BUILD_TESTING)
  add_executable(sheet_output_scene_tests tests/sheet_output_scene_tests.cpp)
  target_link_libraries(sheet_output_scene_tests PRIVATE sketch_sheet_output_scene sketch_storage)
  add_test(NAME sheet_output_scene COMMAND sheet_output_scene_tests)
endif()
add_library(sketch_storage src/core/project_store.cpp)
target_include_directories(sketch_storage PUBLIC include)
target_link_libraries(sketch_storage PUBLIC sketch_document sketch_recovery_ledger PRIVATE sketch_sqlite bcrypt)
add_library(sketch_recovery_discovery src/core/recovery_discovery.cpp)
target_include_directories(sketch_recovery_discovery PUBLIC include)
target_link_libraries(sketch_recovery_discovery PUBLIC sketch_storage)
add_library(sketch_offline_policy src/core/offline_policy.cpp)
target_include_directories(sketch_offline_policy PUBLIC include)
target_link_libraries(sketch_offline_policy PUBLIC sketch_json)
add_library(sketch_schedule_model src/core/schedule_model.cpp)
target_include_directories(sketch_schedule_model PUBLIC include)
add_library(sketch_geometry_operations src/core/geometry_operations.cpp)
target_include_directories(sketch_geometry_operations PUBLIC include)
target_link_libraries(sketch_geometry_operations PUBLIC sketch_document sketch_precision)
add_library(sketch_reference_asset src/core/reference_asset.cpp)
target_include_directories(sketch_reference_asset PUBLIC include)
target_link_libraries(sketch_reference_asset PUBLIC sketch_document sketch_precision sketch_json)
add_library(sketch_workspace_ownership src/core/workspace_ownership_broker.cpp)
target_include_directories(sketch_workspace_ownership PUBLIC include)
if(MSVC)
  target_compile_options(sketch_workspace_ownership PRIVATE /W4 /permissive- /utf-8)
endif()
add_library(sketch_project_ownership src/core/project_ownership.cpp)
target_include_directories(sketch_project_ownership PUBLIC include)
target_link_libraries(sketch_project_ownership PUBLIC sketch_storage sketch_workspace_ownership)
if(MSVC)
  target_compile_options(sketch_project_ownership PRIVATE /W4 /permissive- /utf-8)
endif()
add_library(sketch_exchange src/core/project_exchange.cpp)
target_include_directories(sketch_exchange PUBLIC include)
target_link_libraries(sketch_exchange PUBLIC sketch_document)
add_executable(property-cli src/cli/main.cpp)
target_link_libraries(property-cli PRIVATE sketch_storage sketch_exchange sketch_project_resource_catalog
  sketch_annotation_catalog)
if(MSVC)
  foreach(target IN ITEMS sketch_document sketch_project_organization sketch_project_visibility sketch_output_fingerprint sketch_storage sketch_exchange property-cli)
    target_compile_options(${target} PRIVATE /W4 /permissive- /utf-8)
  endforeach()
endif()

add_library(sketch_precision src/core/geometry.cpp src/core/quantity.cpp)
target_include_directories(sketch_precision PUBLIC include)
add_library(sketch_door_operation src/core/door_operation.cpp)
target_link_libraries(sketch_door_operation PUBLIC sketch_precision sketch_json)
target_link_libraries(sketch_document PRIVATE sketch_door_operation)
add_library(sketch_opening_assembly src/core/opening_assembly.cpp)
target_include_directories(sketch_opening_assembly PUBLIC include)
target_link_libraries(sketch_opening_assembly PUBLIC sketch_json)
if(MSVC)
  target_compile_options(sketch_opening_assembly PRIVATE /W4 /permissive- /utf-8)
endif()
target_link_libraries(sketch_document PRIVATE sketch_opening_assembly)
add_library(sketch_assistance_engine src/core/assistance_engine.cpp)
target_include_directories(sketch_assistance_engine PUBLIC include)
target_link_libraries(sketch_assistance_engine PUBLIC sketch_assistance_contract sketch_precision)
add_library(sketch_document_schedule_adapter src/core/document_schedule_adapter.cpp)
target_include_directories(sketch_document_schedule_adapter PUBLIC include)
target_link_libraries(sketch_document_schedule_adapter PUBLIC sketch_schedule_model sketch_document sketch_precision)
target_link_libraries(sketch_document_schedule_adapter PUBLIC sketch_door_operation)
add_library(sketch_boundary_receipts src/core/boundary_receipt.cpp)
target_link_libraries(sketch_boundary_receipts PUBLIC sketch_precision sketch_json)
add_library(sketch_wall_semantics src/core/wall_semantics.cpp)
target_link_libraries(sketch_wall_semantics PUBLIC sketch_precision sketch_json)
add_library(sketch_roof_join_semantics src/core/roof_join_semantics.cpp)
target_include_directories(sketch_roof_join_semantics PUBLIC include)
target_link_libraries(sketch_roof_join_semantics PUBLIC sketch_json)
add_library(sketch_slab_semantics src/core/slab_semantics.cpp)
target_include_directories(sketch_slab_semantics PUBLIC include)
target_link_libraries(sketch_slab_semantics PUBLIC sketch_wall_semantics sketch_json)
add_library(sketch_boundary_authoring
  src/core/boundary_authoring_session.cpp
  src/core/boundary_authoring_recovery.cpp
  src/core/boundary_authoring_recovery_resource.cpp
  src/core/boundary_construction.cpp)
target_link_libraries(sketch_boundary_authoring PUBLIC sketch_document sketch_boundary_receipts)
add_library(sketch_boundary_commit src/core/boundary_commit.cpp)
add_library(sketch_boundary_recovery_source src/core/boundary_recovery_source.cpp)
target_link_libraries(sketch_boundary_recovery_source PUBLIC sketch_document sketch_project_organization)
add_library(sketch_boundary_active_recovery src/core/boundary_active_recovery.cpp)
target_link_libraries(sketch_boundary_active_recovery PUBLIC sketch_boundary_authoring sketch_boundary_recovery_source)
add_library(sketch_recovery_copy_record src/core/recovery_copy_record.cpp)
add_library(sketch_workspace_history_fence src/core/workspace_history_fence.cpp)
add_library(sketch_project_workspace src/core/project_workspace.cpp)
add_library(sketch_workspace_lifecycle_validation src/core/workspace_lifecycle_validation.cpp)
target_link_libraries(sketch_workspace_lifecycle_validation PUBLIC sketch_project_workspace)
add_library(sketch_workspace_slot_validation src/core/workspace_slot_validation.cpp)
target_link_libraries(sketch_workspace_slot_validation PUBLIC sketch_workspace_lifecycle_validation)
add_library(sketch_workspace_recovery_budget src/core/workspace_recovery_budget.cpp)
target_link_libraries(sketch_workspace_recovery_budget PUBLIC sketch_workspace_slot_validation)
add_library(sketch_workspace_history_record src/core/workspace_history_record.cpp)
target_link_libraries(sketch_workspace_history_record PUBLIC sketch_workspace_recovery_budget)
add_library(sketch_recovery_ledger src/core/recovery_ledger.cpp)
target_link_libraries(sketch_recovery_ledger PUBLIC sketch_workspace_history_record sketch_recovery_copy_record)
add_library(sketch_project_workspace_archive_restore src/core/project_workspace_archive_restore.cpp)
target_link_libraries(sketch_project_workspace_archive_restore PUBLIC sketch_project_workspace sketch_recovery_ledger)
add_library(sketch_workspace_save_coordinator src/core/workspace_save_coordinator.cpp)
target_link_libraries(sketch_workspace_save_coordinator PUBLIC sketch_project_workspace sketch_storage)
add_library(sketch_workspace_save_queue src/core/workspace_save_queue.cpp)
target_include_directories(sketch_workspace_save_queue PUBLIC include)
target_link_libraries(sketch_workspace_save_queue PUBLIC sketch_workspace_save_coordinator)
add_library(sketch_workspace_regeneration_queue src/core/workspace_regeneration_queue.cpp)
target_include_directories(sketch_workspace_regeneration_queue PUBLIC include)
add_library(sketch_performance_telemetry src/core/performance_telemetry.cpp)
target_include_directories(sketch_performance_telemetry PUBLIC include)
add_library(sketch_workspace_autosave_scheduler src/core/workspace_autosave_scheduler.cpp)
target_include_directories(sketch_workspace_autosave_scheduler PUBLIC include)
add_library(sketch_integration_adapter src/core/integration_adapter.cpp)
target_include_directories(sketch_integration_adapter PUBLIC include)
add_library(sketch_typed_relationships src/core/typed_relationships.cpp)
target_include_directories(sketch_typed_relationships PUBLIC include)
target_link_libraries(sketch_typed_relationships PUBLIC sketch_json)
add_library(sketch_room_relationships src/core/room_relationships.cpp
  src/core/room_relationship_geometry.cpp)
target_include_directories(sketch_room_relationships PUBLIC include)
target_link_libraries(sketch_room_relationships PUBLIC sketch_json sketch_precision)
add_library(sketch_room_relationship_geometry_commit src/core/room_relationship_geometry_commit.cpp)
target_include_directories(sketch_room_relationship_geometry_commit PUBLIC include)
target_link_libraries(sketch_room_relationship_geometry_commit PUBLIC sketch_room_relationships
  sketch_document sketch_building_entities sketch_boundary_receipts sketch_wall_semantics
  sketch_constraint_authoring)
add_library(sketch_model_phases src/core/model_phases.cpp)
target_include_directories(sketch_model_phases PUBLIC include)
target_link_libraries(sketch_model_phases PUBLIC sketch_json)
add_library(sketch_import_worker_policy src/core/import_worker_policy.cpp)
target_include_directories(sketch_import_worker_policy PUBLIC include)
target_link_libraries(sketch_import_worker_policy PUBLIC sketch_json)
add_library(sketch_windows_import_worker src/core/windows_import_worker.cpp)
target_include_directories(sketch_windows_import_worker PUBLIC include)
target_link_libraries(sketch_windows_import_worker PUBLIC sketch_json)
if(WIN32)
  target_link_libraries(sketch_windows_import_worker PUBLIC advapi32 userenv)
endif()
add_library(sketch_vertical_levels src/core/vertical_levels.cpp)
target_include_directories(sketch_vertical_levels PUBLIC include)
target_link_libraries(sketch_vertical_levels PUBLIC sketch_typed_relationships sketch_json)
add_library(sketch_reference_grid src/core/reference_grid.cpp)
target_include_directories(sketch_reference_grid PUBLIC include)
target_link_libraries(sketch_reference_grid PUBLIC sketch_json)
add_library(sketch_terrain_surface src/core/terrain_surface.cpp)
target_include_directories(sketch_terrain_surface PUBLIC include)
target_link_libraries(sketch_terrain_surface PUBLIC sketch_precision sketch_json)
add_library(sketch_workspace_document_history src/core/workspace_document_history.cpp)
add_library(sketch_workspace_navigation src/core/workspace_navigation.cpp)
target_link_libraries(sketch_workspace_navigation PUBLIC sketch_workspace_history_fence)
target_link_libraries(sketch_workspace_document_history PUBLIC sketch_workspace_history_fence)
target_link_libraries(sketch_project_workspace PUBLIC sketch_document sketch_workspace_document_history sketch_boundary_active_recovery sketch_workspace_navigation sketch_boundary_commit)
target_link_libraries(sketch_workspace_history_fence PUBLIC sketch_document)
target_link_libraries(sketch_recovery_copy_record PUBLIC sketch_boundary_authoring)
target_link_libraries(sketch_boundary_commit PUBLIC sketch_boundary_authoring sketch_project_organization)
if(MSVC)
  target_compile_options(sketch_precision PRIVATE /W4 /permissive- /utf-8)
  target_compile_options(sketch_wall_semantics PRIVATE /W4 /permissive- /utf-8)
  target_compile_options(sketch_roof_join_semantics PRIVATE /W4 /permissive- /utf-8)
  target_compile_options(sketch_boundary_authoring PRIVATE /W4 /permissive- /utf-8)
  target_compile_options(sketch_boundary_receipts PRIVATE /W4 /permissive- /utf-8)
  target_compile_options(sketch_boundary_commit PRIVATE /W4 /permissive- /utf-8)
  target_compile_options(sketch_boundary_recovery_source PRIVATE /W4 /permissive- /utf-8)
  target_compile_options(sketch_boundary_active_recovery PRIVATE /W4 /permissive- /utf-8)
  target_compile_options(sketch_recovery_copy_record PRIVATE /W4 /permissive- /utf-8)
  target_compile_options(sketch_workspace_history_fence PRIVATE /W4 /permissive- /utf-8)
  target_compile_options(sketch_project_workspace PRIVATE /W4 /permissive- /utf-8)
  target_compile_options(sketch_project_workspace_archive_restore PRIVATE /W4 /permissive- /utf-8)
  target_compile_options(sketch_workspace_lifecycle_validation PRIVATE /W4 /permissive- /utf-8)
  target_compile_options(sketch_workspace_slot_validation PRIVATE /W4 /permissive- /utf-8)
  target_compile_options(sketch_workspace_recovery_budget PRIVATE /W4 /permissive- /utf-8)
  target_compile_options(sketch_workspace_history_record PRIVATE /W4 /permissive- /utf-8)
  target_compile_options(sketch_recovery_ledger PRIVATE /W4 /permissive- /utf-8)
  target_compile_options(sketch_workspace_save_coordinator PRIVATE /W4 /permissive- /utf-8)
  target_compile_options(sketch_workspace_save_queue PRIVATE /W4 /permissive- /utf-8)
  target_compile_options(sketch_workspace_regeneration_queue PRIVATE /W4 /permissive- /utf-8)
  target_compile_options(sketch_performance_telemetry PRIVATE /W4 /permissive- /utf-8)
  target_compile_options(sketch_workspace_autosave_scheduler PRIVATE /W4 /permissive- /utf-8)
  target_compile_options(sketch_recovery_discovery PRIVATE /W4 /permissive- /utf-8)
  target_compile_options(sketch_offline_policy PRIVATE /W4 /permissive- /utf-8)
  target_compile_options(sketch_schedule_model PRIVATE /W4 /permissive- /utf-8)
  target_compile_options(sketch_document_schedule_adapter PRIVATE /W4 /permissive- /utf-8)
  target_compile_options(sketch_geometry_operations PRIVATE /W4 /permissive- /utf-8)
  target_compile_options(sketch_reference_asset PRIVATE /W4 /permissive- /utf-8)
  target_compile_options(sketch_integration_adapter PRIVATE /W4 /permissive- /utf-8)
  target_compile_options(sketch_typed_relationships PRIVATE /W4 /permissive- /utf-8)
  target_compile_options(sketch_room_relationships PRIVATE /W4 /permissive- /utf-8)
  target_compile_options(sketch_room_relationship_geometry_commit PRIVATE /W4 /permissive- /utf-8)
  target_compile_options(sketch_model_phases PRIVATE /W4 /permissive- /utf-8)
  target_compile_options(sketch_import_worker_policy PRIVATE /W4 /permissive- /utf-8)
  target_compile_options(sketch_windows_import_worker PRIVATE /W4 /permissive- /utf-8)
  target_compile_options(sketch_annotation_catalog PRIVATE /W4 /permissive- /utf-8)
  target_compile_options(sketch_document PRIVATE /W4 /permissive- /utf-8)
  target_compile_options(sketch_sheet_view_model PRIVATE /W4 /permissive- /utf-8)
  target_compile_options(sketch_interchange_profile PRIVATE /W4 /permissive- /utf-8)
  target_compile_options(sketch_field_adapter_contract PRIVATE /W4 /permissive- /utf-8)
  target_compile_options(sketch_assistance_contract PRIVATE /W4 /permissive- /utf-8)
  target_compile_options(sketch_assistance_engine PRIVATE /W4 /permissive- /utf-8)
  target_compile_options(sketch_multipage_document PRIVATE /W4 /permissive- /utf-8)
  target_compile_options(sketch_assembly_model PRIVATE /W4 /permissive- /utf-8)
  target_compile_options(sketch_product_scope PRIVATE /W4 /permissive- /utf-8)
  target_compile_options(sketch_workspace_accessibility PRIVATE /W4 /permissive- /utf-8)
  target_compile_options(sketch_survey_contract PRIVATE /W4 /permissive- /utf-8)
  target_compile_options(sketch_georeferencing_contract PRIVATE /W4 /permissive- /utf-8)
  if(TARGET sketch_georeferencing_runtime)
    target_compile_options(sketch_georeferencing_runtime PRIVATE /W4 /permissive- /utf-8)
  endif()
  target_compile_options(sketch_architectural_workflow_contract PRIVATE /W4 /permissive- /utf-8)
  target_compile_options(sketch_architectural_document_adapter PRIVATE /W4 /permissive- /utf-8)
  target_compile_options(sketch_vertical_levels PRIVATE /W4 /permissive- /utf-8)
  target_compile_options(sketch_reference_grid PRIVATE /W4 /permissive- /utf-8)
  target_compile_options(sketch_terrain_surface PRIVATE /W4 /permissive- /utf-8)
  target_compile_options(sketch_workspace_document_history PRIVATE /W4 /permissive- /utf-8)
  target_compile_options(sketch_workspace_navigation PRIVATE /W4 /permissive- /utf-8)
else()
  target_compile_options(sketch_precision PRIVATE -Wall -Wextra -Wpedantic)
endif()

if(SKETCH_BUILD_ARCHITECTURE OR SKETCH_BUILD_DESKTOP)
  include(cmake/PlaneGCS.cmake)
  add_library(sketch_constraint_authoring src/core/constraint_authoring.cpp)
  target_link_libraries(sketch_constraint_authoring PUBLIC sketch_document sketch_constraints sketch_precision sketch_project_organization)
  if(MSVC)
    target_compile_options(sketch_constraint_authoring PRIVATE /W4 /permissive- /utf-8)
  endif()
  if(BUILD_TESTING)
    add_executable(constraint_authoring_tests tests/constraint_authoring_tests.cpp)
    target_link_libraries(constraint_authoring_tests PRIVATE sketch_constraint_authoring sketch_storage)
    add_test(NAME constraint_authoring COMMAND constraint_authoring_tests)
  endif()
  add_library(sketch_project_workspace_preview_adapters src/core/project_workspace_preview_adapters.cpp)
  target_include_directories(sketch_project_workspace_preview_adapters PUBLIC include)
  target_link_libraries(sketch_project_workspace_preview_adapters PUBLIC
    sketch_project_workspace sketch_constraint_authoring sketch_boundary_commit)
  if(MSVC)
    target_compile_options(sketch_project_workspace_preview_adapters PRIVATE /W4 /permissive- /utf-8)
  endif()
  if(BUILD_TESTING)
    add_executable(project_workspace_preview_adapters_tests tests/project_workspace_preview_adapters_tests.cpp)
    target_link_libraries(project_workspace_preview_adapters_tests PRIVATE sketch_project_workspace_preview_adapters sketch_storage)
    if(MSVC)
      target_compile_options(project_workspace_preview_adapters_tests PRIVATE /W4 /permissive- /utf-8)
    endif()
    add_test(NAME project_workspace_preview_adapters COMMAND project_workspace_preview_adapters_tests)
  endif()
endif()

if(SKETCH_BUILD_DESKTOP)
  find_package(Qt6 6.8 CONFIG REQUIRED COMPONENTS Core Gui Widgets OpenGL OpenGLWidgets PrintSupport Pdf Svg)
  add_library(sketch_reference_import src/desktop/reference_import.cpp)
  target_include_directories(sketch_reference_import PUBLIC src/desktop)
  target_link_libraries(sketch_reference_import PUBLIC sketch_windows_import_worker Qt6::Gui)
  add_executable(property-studio-import-worker src/desktop/reference_import_worker.cpp)
  target_include_directories(property-studio-import-worker PRIVATE include)
  target_link_libraries(property-studio-import-worker PRIVATE sketch_json Qt6::Gui Qt6::Pdf)
  if(WIN32)
    target_link_libraries(property-studio-import-worker PRIVATE windowscodecs ole32)
  endif()
  if(MSVC)
    target_compile_options(sketch_reference_import PRIVATE /W4 /permissive- /utf-8)
    target_compile_options(property-studio-import-worker PRIVATE /W4 /permissive- /utf-8)
  endif()
  add_library(sketch_visualization src/visualization/native_model_view.cpp
    src/visualization/native_geometry_preparation.cpp)
  target_include_directories(sketch_visualization PUBLIC include)
  target_link_libraries(sketch_visualization PUBLIC sketch_architecture sketch_building_entities sketch_document sketch_project_organization sketch_project_visibility sketch_architectural_workflow_contract Qt6::Widgets TKService TKV3d TKOpenGl TKMesh)
  add_library(sketch_plan_canvas src/desktop/plan_canvas.cpp)
  target_include_directories(sketch_plan_canvas PUBLIC include PRIVATE src/desktop)
  target_link_libraries(sketch_plan_canvas PUBLIC Qt6::Widgets sketch_precision
    sketch_reference_grid)
  add_library(sketch_boundary_input src/desktop/boundary_input_dialog.cpp)
  target_include_directories(sketch_boundary_input PUBLIC include)
  target_link_libraries(sketch_boundary_input PUBLIC sketch_boundary_authoring Qt6::Widgets)
  add_library(sketch_desktop src/desktop/main_window.cpp
    src/desktop/building_object_dialog.cpp src/desktop/hosted_opening_dialog.cpp src/desktop/constraint_dialog.cpp
    src/desktop/constraint_preview_canvas.cpp)
  target_include_directories(sketch_desktop PUBLIC include PRIVATE src/desktop)
  target_link_libraries(sketch_desktop PUBLIC sketch_reference_import)
  add_dependencies(sketch_desktop property-studio-import-worker)
  target_link_libraries(sketch_desktop PUBLIC sketch_survey_contract sketch_storage sketch_project_ownership sketch_recovery_discovery sketch_project_organization sketch_project_visibility sketch_precision sketch_geometry_operations sketch_assistance_engine sketch_constraint_authoring sketch_room_relationship_geometry_commit sketch_calculations sketch_schedule_model sketch_document_schedule_adapter sketch_architectural_schedule sketch_visualization sketch_building_plan_projection sketch_plan_canvas sketch_boundary_input sketch_boundary_commit sketch_project_workspace_archive_restore sketch_project_workspace_preview_adapters sketch_workspace_save_coordinator sketch_workspace_save_queue sketch_workspace_regeneration_queue sketch_performance_telemetry sketch_workspace_autosave_scheduler sketch_offline_policy sketch_field_adapter_contract sketch_workspace_accessibility sketch_architectural_document_adapter sketch_sheet_output_scene sketch_output_fingerprint sketch_dxf_project_exchange sketch_ifc_project_exchange sketch_georeferencing_runtime sketch_project_resource_catalog Qt6::Widgets Qt6::PrintSupport Qt6::Pdf Qt6::Svg)
  qt_add_executable(property-studio WIN32 src/desktop/main.cpp)
  qt_add_resources(property-studio application_fonts PREFIX "/fonts" BASE assets/fonts
    FILES assets/fonts/Inter.ttf assets/fonts/OFL.txt)
  target_link_libraries(property-studio PRIVATE sketch_desktop)
  if(BUILD_TESTING)
    add_executable(reference_import_tests tests/reference_import_tests.cpp)
    target_link_libraries(reference_import_tests PRIVATE sketch_reference_import)
    add_dependencies(reference_import_tests property-studio-import-worker)
    add_test(NAME reference_import COMMAND reference_import_tests "$<TARGET_FILE:property-studio-import-worker>")
  endif()
  if(MSVC)
    target_compile_options(sketch_visualization PRIVATE /W4 /permissive- /utf-8)
    target_compile_options(sketch_plan_canvas PRIVATE /W4 /permissive- /utf-8)
    target_compile_options(sketch_boundary_input PRIVATE /W4 /permissive- /utf-8)
    target_compile_options(sketch_desktop PRIVATE /W4 /permissive- /utf-8 /bigobj)
    target_compile_options(property-studio PRIVATE /W4 /permissive- /utf-8)
  endif()
  if(BUILD_TESTING)
    # Hardware/native-window checks run explicitly; ordinary CTest stays offscreen.
    add_executable(native_view_tests tests/native_view_tests.cpp)
    target_link_libraries(native_view_tests PRIVATE sketch_visualization sketch_constraint_authoring)
    add_executable(native_geometry_preparation_tests tests/native_geometry_preparation_tests.cpp)
    target_link_libraries(native_geometry_preparation_tests PRIVATE sketch_visualization sketch_workspace_regeneration_queue)
    add_executable(native_model_preparation_tests tests/native_model_preparation_tests.cpp)
    target_link_libraries(native_model_preparation_tests PRIVATE sketch_visualization)
    add_executable(performance_workload_tests tests/performance_workload_tests.cpp)
    target_link_libraries(performance_workload_tests PRIVATE sketch_performance_workload sketch_storage Qt6::Gui)
    if(MSVC)
      target_compile_options(native_geometry_preparation_tests PRIVATE /W4 /permissive- /utf-8)
      target_compile_options(native_model_preparation_tests PRIVATE /W4 /permissive- /utf-8)
      target_compile_options(performance_workload_tests PRIVATE /W4 /permissive- /utf-8)
    endif()
    add_test(NAME native_geometry_preparation COMMAND native_geometry_preparation_tests)
    add_test(NAME native_model_preparation COMMAND native_model_preparation_tests)
    add_test(NAME performance_workload COMMAND performance_workload_tests)
    add_executable(desktop_smoke tests/desktop_smoke.cpp)
    target_link_libraries(desktop_smoke PRIVATE sketch_desktop sketch_product_scope)
    qt_add_resources(desktop_smoke desktop_smoke_test_fonts PREFIX "/fonts" BASE assets/fonts
      FILES assets/fonts/Inter.ttf)
    add_executable(project_resource_desktop_tests tests/project_resource_desktop_tests.cpp)
    target_link_libraries(project_resource_desktop_tests PRIVATE sketch_desktop)
    add_executable(desktop_performance_tests tests/desktop_performance_tests.cpp)
    target_link_libraries(desktop_performance_tests PRIVATE sketch_desktop)
    add_executable(desktop_workspace_recovery_tests tests/desktop_workspace_recovery_tests.cpp)
    target_link_libraries(desktop_workspace_recovery_tests PRIVATE sketch_desktop)
    add_executable(building_object_dialog_tests tests/building_object_dialog_tests.cpp)
    target_link_libraries(building_object_dialog_tests PRIVATE sketch_desktop)
    add_executable(building_receipt_workflow_tests tests/building_receipt_workflow_tests.cpp)
    target_link_libraries(building_receipt_workflow_tests PRIVATE sketch_desktop)
    add_executable(modal_authoring_tests tests/modal_authoring_tests.cpp)
    target_link_libraries(modal_authoring_tests PRIVATE sketch_desktop)
    add_executable(constraint_dialog_tests tests/constraint_dialog_tests.cpp)
    target_link_libraries(constraint_dialog_tests PRIVATE sketch_desktop)
    add_executable(visibility_workflow_tests tests/visibility_workflow_tests.cpp)
    target_link_libraries(visibility_workflow_tests PRIVATE sketch_desktop)
    add_executable(boundary_canvas_tests tests/boundary_canvas_tests.cpp)
    target_link_libraries(boundary_canvas_tests PRIVATE sketch_plan_canvas)
    add_executable(boundary_input_dialog_tests tests/boundary_input_dialog_tests.cpp)
    target_link_libraries(boundary_input_dialog_tests PRIVATE sketch_boundary_input)
    add_executable(boundary_workflow_tests tests/boundary_workflow_tests.cpp)
    target_link_libraries(boundary_workflow_tests PRIVATE sketch_desktop)
    add_executable(assistance_workflow_tests tests/assistance_workflow_tests.cpp)
    target_link_libraries(assistance_workflow_tests PRIVATE sketch_desktop)
    add_executable(dxf_desktop_workflow_tests tests/dxf_desktop_workflow_tests.cpp)
    target_link_libraries(dxf_desktop_workflow_tests PRIVATE sketch_desktop)
    add_executable(ifc_desktop_workflow_tests tests/ifc_desktop_workflow_tests.cpp)
    target_link_libraries(ifc_desktop_workflow_tests PRIVATE sketch_desktop)
    qt_add_resources(boundary_workflow_tests boundary_workflow_test_fonts PREFIX "/fonts" BASE assets/fonts
      FILES assets/fonts/Inter.ttf)
    qt_add_resources(boundary_canvas_tests boundary_canvas_test_fonts PREFIX "/fonts" BASE assets/fonts
      FILES assets/fonts/Inter.ttf)
    qt_add_resources(boundary_input_dialog_tests boundary_input_test_fonts PREFIX "/fonts" BASE assets/fonts
      FILES assets/fonts/Inter.ttf)
    qt_add_resources(visibility_workflow_tests visibility_test_fonts PREFIX "/fonts" BASE assets/fonts
      FILES assets/fonts/Inter.ttf)
    qt_add_resources(dxf_desktop_workflow_tests dxf_workflow_test_fonts PREFIX "/fonts" BASE assets/fonts
      FILES assets/fonts/Inter.ttf)
    qt_add_resources(ifc_desktop_workflow_tests ifc_workflow_test_fonts PREFIX "/fonts" BASE assets/fonts
      FILES assets/fonts/Inter.ttf)
    qt_add_resources(constraint_dialog_tests constraint_test_fonts PREFIX "/fonts" BASE assets/fonts
      FILES assets/fonts/Inter.ttf)
    if(MSVC)
      target_compile_options(desktop_smoke PRIVATE /utf-8)
      target_compile_options(project_resource_desktop_tests PRIVATE /W4 /permissive- /utf-8)
      target_compile_options(desktop_performance_tests PRIVATE /W4 /permissive- /utf-8)
      target_compile_options(desktop_workspace_recovery_tests PRIVATE /W4 /permissive- /utf-8)
      target_compile_options(building_object_dialog_tests PRIVATE /W4 /permissive- /utf-8)
      target_compile_options(building_receipt_workflow_tests PRIVATE /W4 /permissive- /utf-8)
      target_compile_options(modal_authoring_tests PRIVATE /W4 /permissive- /utf-8)
      target_compile_options(constraint_dialog_tests PRIVATE /W4 /permissive- /utf-8)
      target_compile_options(visibility_workflow_tests PRIVATE /W4 /permissive- /utf-8)
      target_compile_options(boundary_canvas_tests PRIVATE /W4 /permissive- /utf-8)
      target_compile_options(boundary_input_dialog_tests PRIVATE /W4 /permissive- /utf-8)
      target_compile_options(boundary_workflow_tests PRIVATE /W4 /permissive- /utf-8)
      target_compile_options(assistance_workflow_tests PRIVATE /W4 /permissive- /utf-8)
      target_compile_options(dxf_desktop_workflow_tests PRIVATE /W4 /permissive- /utf-8)
      target_compile_options(ifc_desktop_workflow_tests PRIVATE /W4 /permissive- /utf-8)
    endif()
    add_test(NAME desktop_workflow COMMAND desktop_smoke)
    add_test(NAME project_resource_desktop COMMAND project_resource_desktop_tests)
    add_test(NAME desktop_performance COMMAND desktop_performance_tests)
    add_test(NAME desktop_workspace_recovery COMMAND desktop_workspace_recovery_tests)
    add_test(NAME building_object_dialog COMMAND building_object_dialog_tests)
    add_test(NAME building_receipt_workflow COMMAND building_receipt_workflow_tests)
    add_test(NAME modal_authoring COMMAND modal_authoring_tests)
    add_test(NAME constraint_dialog COMMAND constraint_dialog_tests)
    add_test(NAME visibility_workflow COMMAND visibility_workflow_tests)
    add_test(NAME boundary_canvas COMMAND boundary_canvas_tests)
    add_test(NAME boundary_input_dialog COMMAND boundary_input_dialog_tests)
    add_test(NAME boundary_workflow COMMAND boundary_workflow_tests)
    add_test(NAME assistance_workflow COMMAND assistance_workflow_tests)
    add_test(NAME dxf_desktop_workflow COMMAND dxf_desktop_workflow_tests)
    add_test(NAME ifc_desktop_workflow COMMAND ifc_desktop_workflow_tests)
    get_target_property(SKETCH_QMAKE_EXECUTABLE Qt6::qmake IMPORTED_LOCATION)
    get_filename_component(SKETCH_QT_BIN "${SKETCH_QMAKE_EXECUTABLE}" DIRECTORY)
    get_filename_component(SKETCH_QT_PREFIX "${SKETCH_QT_BIN}" DIRECTORY)
    set_tests_properties(reference_import desktop_workflow desktop_performance native_geometry_preparation native_model_preparation performance_workload project_resource_desktop desktop_workspace_recovery building_object_dialog building_receipt_workflow modal_authoring constraint_dialog visibility_workflow boundary_canvas boundary_input_dialog boundary_workflow assistance_workflow dxf_desktop_workflow ifc_desktop_workflow PROPERTIES
      ENVIRONMENT "QT_QPA_PLATFORM=offscreen;QT_PLUGIN_PATH=${SKETCH_QT_PREFIX}/plugins"
      ENVIRONMENT_MODIFICATION "PATH=path_list_prepend:${SKETCH_QT_BIN};PATH=path_list_prepend:${CMAKE_SOURCE_DIR}/.deps/native/x64-windows/$<IF:$<CONFIG:Debug>,debug/bin,bin>")
  endif()
endif()

if(BUILD_TESTING)
  find_package(Python3 REQUIRED COMPONENTS Interpreter)
  add_test(NAME reference_import_boundary COMMAND "${Python3_EXECUTABLE}" -m unittest discover
    -s "${CMAKE_SOURCE_DIR}/tests" -p test_reference_import_boundary.py)
  if(WIN32)
    find_program(SKETCH_TEST_POWERSHELL NAMES pwsh powershell REQUIRED)
    add_test(NAME native_process_guard COMMAND "${SKETCH_TEST_POWERSHELL}"
      -NoLogo -NoProfile -NonInteractive -File "${CMAKE_SOURCE_DIR}/tests/test_native_process_guard.ps1")
  endif()
  add_executable(error_policy_probe tests/error_policy_probe.cpp)
  add_test(NAME noninteractive_test_errors COMMAND "${Python3_EXECUTABLE}" "${CMAKE_SOURCE_DIR}/tests/test_error_policy.py" "$<TARGET_FILE:error_policy_probe>")
  add_test(NAME dependency_integrity COMMAND "${Python3_EXECUTABLE}" -m unittest discover -s "${CMAKE_SOURCE_DIR}/tests" -p test_bootstrap.py)
  # These validate report parsing and threshold summaries, not production
  # performance qualification. Run them in both build configurations.
  foreach(performance_test IN ITEMS benchmark report qualification)
    add_test(NAME "performance_${performance_test}_contract" COMMAND "${Python3_EXECUTABLE}" -m unittest discover
      -s "${CMAKE_SOURCE_DIR}/tests" -p "test_performance_${performance_test}.py")
  endforeach()
  foreach(packaging_test IN ITEMS source_kit_manifest update_source_kit_allowlist distribution_inventory distribution_sbom stage_portable_package stage_project_package stage_offline_bundle handoff_inventory_audit prepare_msvc_runtime installed_runtime offline_static_audit source_provenance_audit symbol_catalog_manifest)
    add_test(NAME "packaging_${packaging_test}" COMMAND "${Python3_EXECUTABLE}" -m unittest discover
      -s "${CMAKE_SOURCE_DIR}/tests" -p "test_${packaging_test}.py")
  endforeach()
  # A delivered source kit intentionally contains no Git metadata. Its build
  # must not query an unrelated parent checkout's index.
  if(EXISTS "${CMAKE_SOURCE_DIR}/.git")
    add_test(NAME source_kit_allowlist_contract COMMAND "${Python3_EXECUTABLE}"
      "${CMAKE_SOURCE_DIR}/scripts/update_source_kit_allowlist.py" --source-root "${CMAKE_SOURCE_DIR}" --check)
  endif()
  # Synthetic audit-contract tests are deliberately separate from the real
  # production acceptance gate below.
  add_test(NAME requirement_audit_logic COMMAND "${Python3_EXECUTABLE}" -m unittest discover -s "${CMAKE_SOURCE_DIR}/tests" -p test_requirement_audit.py)
  # Keep the default test-suite explicit: this checks the requirement/gate
  # schema only. Release evidence is evaluated by the separate disabled gate.
  add_test(NAME requirement_schema_contract COMMAND "${Python3_EXECUTABLE}" "${CMAKE_SOURCE_DIR}/scripts/requirement_audit.py" --contract)
  # The evidence manifest contract is part of the default verification matrix.
  # It remains fail-closed and never certifies runtime behavior by itself.
  add_test(NAME production_qualification_contract COMMAND "${Python3_EXECUTABLE}" -m unittest discover
    -s "${CMAKE_SOURCE_DIR}/tests" -p test_production_qualification.py)
  # The checkpoint checklist is a report contract; it must not become a
  # substitute for the explicitly disabled production acceptance gate.
  add_test(NAME completion_audit_contract COMMAND "${Python3_EXECUTABLE}" -m unittest discover
    -s "${CMAKE_SOURCE_DIR}/tests" -p test_completion_audit.py)
  # Hash-bound architecture workflow evidence is generated from the same
  # source and CTest records used by the release audit.
  add_test(NAME workflow_acceptance_evidence_contract COMMAND "${Python3_EXECUTABLE}" -m unittest discover
    -s "${CMAKE_SOURCE_DIR}/tests" -p test_workflow_acceptance_evidence.py)
  # Explicit production-acceptance gate: fails closed with --release and must be
  # run explicitly via a dedicated CI/lifecycle step.
  add_test(NAME production_acceptance_gate COMMAND "${Python3_EXECUTABLE}" "${CMAKE_SOURCE_DIR}/scripts/requirement_audit.py" --release)
  set_tests_properties(production_acceptance_gate PROPERTIES LABELS "production" DISABLED TRUE)
  foreach(name IN ITEMS geometry quantity)
    add_executable(${name}_tests tests/${name}_tests.cpp)
    target_link_libraries(${name}_tests PRIVATE sketch_precision)
    add_test(NAME ${name} COMMAND ${name}_tests)
  endforeach()
  add_executable(curve_construction_tests tests/curve_construction_tests.cpp)
  target_link_libraries(curve_construction_tests PRIVATE sketch_precision)
  add_test(NAME curve_construction COMMAND curve_construction_tests)
  add_executable(document_tests tests/document_tests.cpp)
  add_executable(document_digest_tests tests/document_digest_tests.cpp)
  target_link_libraries(document_digest_tests PRIVATE sketch_document)
  add_test(NAME document_digest COMMAND document_digest_tests)
  add_executable(boundary_integrity_tests tests/boundary_integrity_tests.cpp)
  target_link_libraries(boundary_integrity_tests PRIVATE sketch_storage sketch_sqlite)
  if(MSVC)
    target_compile_options(boundary_integrity_tests PRIVATE /W4 /permissive- /utf-8)
  endif()
  add_test(NAME boundary_integrity COMMAND boundary_integrity_tests)
  add_executable(boundary_entity_tests tests/boundary_entity_tests.cpp)
  target_link_libraries(boundary_entity_tests PRIVATE sketch_document)
  if(MSVC)
    target_compile_options(boundary_entity_tests PRIVATE /W4 /permissive- /utf-8)
  endif()
  add_test(NAME boundary_entities COMMAND boundary_entity_tests)
  add_executable(boundary_dimension_tests tests/boundary_dimension_tests.cpp)
  target_link_libraries(boundary_dimension_tests PRIVATE sketch_document)
  if(MSVC)
    target_compile_options(boundary_dimension_tests PRIVATE /W4 /permissive- /utf-8)
  endif()
  add_test(NAME boundary_dimensions COMMAND boundary_dimension_tests)
  add_executable(boundary_authoring_session_tests tests/boundary_authoring_session_tests.cpp)
  add_executable(persistent_sequence_tests tests/persistent_sequence_tests.cpp)
  target_include_directories(persistent_sequence_tests PRIVATE include)
  if(MSVC)
    target_compile_options(persistent_sequence_tests PRIVATE /W4 /permissive- /utf-8)
  endif()
  add_test(NAME persistent_sequence COMMAND persistent_sequence_tests)
  target_link_libraries(boundary_authoring_session_tests PRIVATE sketch_boundary_authoring)
  if(MSVC)
    target_compile_options(boundary_authoring_session_tests PRIVATE /W4 /permissive- /utf-8)
  endif()
  add_test(NAME boundary_authoring_session COMMAND boundary_authoring_session_tests)
  add_executable(boundary_authoring_recovery_tests tests/boundary_authoring_recovery_tests.cpp)
  target_link_libraries(boundary_authoring_recovery_tests PRIVATE sketch_boundary_authoring)
  if(MSVC)
    target_compile_options(boundary_authoring_recovery_tests PRIVATE /W4 /permissive- /utf-8)
  endif()
  add_test(NAME boundary_authoring_recovery COMMAND boundary_authoring_recovery_tests)
  add_executable(boundary_authoring_recovery_resource_tests tests/boundary_authoring_recovery_resource_tests.cpp)
  target_link_libraries(boundary_authoring_recovery_resource_tests PRIVATE sketch_boundary_authoring)
  if(MSVC)
    target_compile_options(boundary_authoring_recovery_resource_tests PRIVATE /W4 /permissive- /utf-8)
  endif()
  add_test(NAME boundary_authoring_recovery_resource COMMAND boundary_authoring_recovery_resource_tests)
  add_executable(boundary_recovery_scaling_tests tests/boundary_recovery_scaling_tests.cpp)
  target_link_libraries(boundary_recovery_scaling_tests PRIVATE sketch_boundary_authoring)
  if(MSVC)
    target_compile_options(boundary_recovery_scaling_tests PRIVATE /W4 /permissive- /utf-8)
  endif()
  add_test(NAME boundary_recovery_scaling COMMAND boundary_recovery_scaling_tests)
  add_executable(boundary_history_structure_tests tests/boundary_history_structure_tests.cpp)
  target_link_libraries(boundary_history_structure_tests PRIVATE sketch_boundary_authoring)
  if(MSVC)
    target_compile_options(boundary_history_structure_tests PRIVATE /W4 /permissive- /utf-8)
  endif()
  add_test(NAME boundary_history_structure COMMAND boundary_history_structure_tests)
  add_executable(boundary_authoring_admission_tests tests/boundary_authoring_admission_tests.cpp)
  target_link_libraries(boundary_authoring_admission_tests PRIVATE sketch_boundary_authoring)
  if(MSVC)
    target_compile_options(boundary_authoring_admission_tests PRIVATE /W4 /permissive- /utf-8)
  endif()
  add_test(NAME boundary_authoring_admission COMMAND boundary_authoring_admission_tests)
  add_executable(boundary_authoring_return_safety_tests tests/boundary_authoring_return_safety_tests.cpp)
  target_link_libraries(boundary_authoring_return_safety_tests PRIVATE sketch_boundary_authoring)
  if(MSVC)
    target_compile_options(boundary_authoring_return_safety_tests PRIVATE /W4 /permissive- /utf-8)
  endif()
  add_test(NAME boundary_authoring_return_safety COMMAND boundary_authoring_return_safety_tests)
  add_executable(boundary_recovery_reference_tests tests/boundary_recovery_reference_tests.cpp)
  target_link_libraries(boundary_recovery_reference_tests PRIVATE sketch_boundary_authoring)
  if(MSVC)
    target_compile_options(boundary_recovery_reference_tests PRIVATE /W4 /permissive- /utf-8)
  endif()
  add_test(NAME boundary_recovery_reference COMMAND boundary_recovery_reference_tests
    "${CMAKE_SOURCE_DIR}/tests/fixtures/boundary-recovery-v24a.json")
  add_executable(workspace_ownership_broker_tests tests/workspace_ownership_broker_tests.cpp)
  target_link_libraries(workspace_ownership_broker_tests PRIVATE sketch_workspace_ownership)
  if(MSVC)
    target_compile_options(workspace_ownership_broker_tests PRIVATE /W4 /permissive- /utf-8)
  endif()
  add_test(NAME workspace_ownership_broker COMMAND workspace_ownership_broker_tests)
  add_executable(project_ownership_tests tests/project_ownership_tests.cpp)
  target_link_libraries(project_ownership_tests PRIVATE sketch_project_ownership)
  if(MSVC)
    target_compile_options(project_ownership_tests PRIVATE /W4 /permissive- /utf-8)
  endif()
  add_test(NAME project_ownership COMMAND project_ownership_tests)
  if(WIN32)
    add_executable(project_ownership_process_tests tests/project_ownership_process_tests.cpp)
    target_link_libraries(project_ownership_process_tests PRIVATE sketch_project_ownership)
    if(MSVC)
      target_compile_options(project_ownership_process_tests PRIVATE /W4 /permissive- /utf-8)
    endif()
    add_test(NAME project_ownership_process COMMAND project_ownership_process_tests)
    add_executable(workspace_ownership_process_tests tests/workspace_ownership_process_tests.cpp)
    target_link_libraries(workspace_ownership_process_tests PRIVATE sketch_workspace_ownership)
    if(MSVC)
      target_compile_options(workspace_ownership_process_tests PRIVATE /W4 /permissive- /utf-8)
    endif()
    add_test(NAME workspace_ownership_process COMMAND workspace_ownership_process_tests)
  endif()
  add_executable(boundary_recovery_source_tests tests/boundary_recovery_source_tests.cpp)
  target_link_libraries(boundary_recovery_source_tests PRIVATE sketch_boundary_active_recovery)
  if(MSVC)
    target_compile_options(boundary_recovery_source_tests PRIVATE /W4 /permissive- /utf-8)
  endif()
  add_test(NAME boundary_recovery_source COMMAND boundary_recovery_source_tests)
  add_executable(boundary_active_recovery_tests tests/boundary_active_recovery_tests.cpp)
  target_link_libraries(boundary_active_recovery_tests PRIVATE sketch_boundary_active_recovery)
  if(MSVC)
    target_compile_options(boundary_active_recovery_tests PRIVATE /W4 /permissive- /utf-8)
  endif()
  add_test(NAME boundary_active_recovery COMMAND boundary_active_recovery_tests)
  add_executable(recovery_copy_record_tests tests/recovery_copy_record_tests.cpp)
  target_link_libraries(recovery_copy_record_tests PRIVATE sketch_recovery_copy_record)
  if(MSVC)
    target_compile_options(recovery_copy_record_tests PRIVATE /W4 /permissive- /utf-8)
  endif()
  add_test(NAME recovery_copy_record COMMAND recovery_copy_record_tests)
  add_executable(workspace_history_fence_tests tests/workspace_history_fence_tests.cpp)
  target_link_libraries(workspace_history_fence_tests PRIVATE sketch_workspace_history_fence)
  add_test(NAME workspace_history_fence COMMAND workspace_history_fence_tests)
  add_executable(project_workspace_tests tests/project_workspace_tests.cpp)
  target_link_libraries(project_workspace_tests PRIVATE sketch_project_workspace)
  if(MSVC)
    target_compile_options(project_workspace_tests PRIVATE /W4 /permissive- /utf-8)
  endif()
  add_test(NAME project_workspace COMMAND project_workspace_tests)
  add_executable(project_workspace_active_tests tests/project_workspace_active_tests.cpp)
  target_link_libraries(project_workspace_active_tests PRIVATE sketch_project_workspace)
  if(MSVC)
    target_compile_options(project_workspace_active_tests PRIVATE /W4 /permissive- /utf-8)
  endif()
  add_test(NAME project_workspace_active COMMAND project_workspace_active_tests)
  add_executable(project_workspace_discard_tests tests/project_workspace_discard_tests.cpp)
  target_link_libraries(project_workspace_discard_tests PRIVATE sketch_project_workspace)
  if(MSVC)
    target_compile_options(project_workspace_discard_tests PRIVATE /W4 /permissive- /utf-8)
  endif()
  add_test(NAME project_workspace_discard COMMAND project_workspace_discard_tests)
  add_executable(project_workspace_finish_tests tests/project_workspace_finish_tests.cpp)
  target_link_libraries(project_workspace_finish_tests PRIVATE sketch_workspace_slot_validation)
  if(MSVC)
    target_compile_options(project_workspace_finish_tests PRIVATE /W4 /permissive- /utf-8)
  endif()
  add_test(NAME project_workspace_finish COMMAND project_workspace_finish_tests)
  add_executable(workspace_lifecycle_validation_tests tests/workspace_lifecycle_validation_tests.cpp)
  target_link_libraries(workspace_lifecycle_validation_tests PRIVATE sketch_workspace_slot_validation)
  if(MSVC)
    target_compile_options(workspace_lifecycle_validation_tests PRIVATE /W4 /permissive- /utf-8)
  endif()
  add_test(NAME workspace_lifecycle_validation COMMAND workspace_lifecycle_validation_tests)
  add_executable(workspace_slot_validation_tests tests/workspace_slot_validation_tests.cpp)
  target_link_libraries(workspace_slot_validation_tests PRIVATE sketch_workspace_slot_validation)
  if(MSVC)
    target_compile_options(workspace_slot_validation_tests PRIVATE /W4 /permissive- /utf-8)
  endif()
  add_test(NAME workspace_slot_validation COMMAND workspace_slot_validation_tests)
  add_executable(workspace_recovery_budget_tests tests/workspace_recovery_budget_tests.cpp)
  target_link_libraries(workspace_recovery_budget_tests PRIVATE sketch_workspace_recovery_budget)
  if(MSVC)
    target_compile_options(workspace_recovery_budget_tests PRIVATE /W4 /permissive- /utf-8)
  endif()
  add_test(NAME workspace_recovery_budget COMMAND workspace_recovery_budget_tests)
  add_executable(workspace_history_record_tests tests/workspace_history_record_tests.cpp)
  target_link_libraries(workspace_history_record_tests PRIVATE sketch_workspace_history_record)
  if(MSVC)
    target_compile_options(workspace_history_record_tests PRIVATE /W4 /permissive- /utf-8)
  endif()
  add_test(NAME workspace_history_record COMMAND workspace_history_record_tests)
  add_executable(recovery_ledger_tests tests/recovery_ledger_tests.cpp)
  target_link_libraries(recovery_ledger_tests PRIVATE sketch_recovery_ledger)
  if(MSVC)
    target_compile_options(recovery_ledger_tests PRIVATE /W4 /permissive- /utf-8)
  endif()
  add_test(NAME recovery_ledger COMMAND recovery_ledger_tests)
  add_executable(recovery_discovery_tests tests/recovery_discovery_tests.cpp)
  target_link_libraries(recovery_discovery_tests PRIVATE sketch_recovery_discovery)
  if(MSVC)
    target_compile_options(recovery_discovery_tests PRIVATE /W4 /permissive- /utf-8)
  endif()
  add_test(NAME recovery_discovery COMMAND recovery_discovery_tests)
  add_executable(offline_policy_tests tests/offline_policy_tests.cpp)
  target_link_libraries(offline_policy_tests PRIVATE sketch_offline_policy)
  if(MSVC)
    target_compile_options(offline_policy_tests PRIVATE /W4 /permissive- /utf-8)
  endif()
  add_test(NAME offline_policy COMMAND offline_policy_tests)
  add_executable(schedule_model_tests tests/schedule_model_tests.cpp)
  target_link_libraries(schedule_model_tests PRIVATE sketch_schedule_model)
  if(MSVC)
    target_compile_options(schedule_model_tests PRIVATE /W4 /permissive- /utf-8)
  endif()
  add_test(NAME schedule_model COMMAND schedule_model_tests)
  add_executable(document_schedule_adapter_tests tests/document_schedule_adapter_tests.cpp)
  target_link_libraries(document_schedule_adapter_tests PRIVATE sketch_document_schedule_adapter)
  if(MSVC)
    target_compile_options(document_schedule_adapter_tests PRIVATE /W4 /permissive- /utf-8)
  endif()
  add_test(NAME document_schedule_adapter COMMAND document_schedule_adapter_tests)
  add_executable(door_operation_tests tests/door_operation_tests.cpp)
  target_link_libraries(door_operation_tests PRIVATE sketch_document_schedule_adapter)
  add_test(NAME door_operation COMMAND door_operation_tests)
  add_executable(geometry_operations_tests tests/geometry_operations_tests.cpp)
  target_link_libraries(geometry_operations_tests PRIVATE sketch_geometry_operations)
  if(MSVC)
    target_compile_options(geometry_operations_tests PRIVATE /W4 /permissive- /utf-8)
  endif()
  add_test(NAME geometry_operations COMMAND geometry_operations_tests)
  add_executable(reference_asset_tests tests/reference_asset_tests.cpp)
  target_link_libraries(reference_asset_tests PRIVATE sketch_reference_asset)
  if(MSVC)
    target_compile_options(reference_asset_tests PRIVATE /W4 /permissive- /utf-8)
  endif()
  add_test(NAME reference_assets COMMAND reference_asset_tests)
  add_executable(vertical_levels_tests tests/vertical_levels_tests.cpp)
  target_link_libraries(vertical_levels_tests PRIVATE sketch_vertical_levels)
  if(MSVC)
    target_compile_options(vertical_levels_tests PRIVATE /W4 /permissive- /utf-8)
  endif()
  add_test(NAME vertical_levels COMMAND vertical_levels_tests)
  add_executable(reference_grid_tests tests/reference_grid_tests.cpp)
  target_link_libraries(reference_grid_tests PRIVATE sketch_reference_grid)
  if(MSVC)
    target_compile_options(reference_grid_tests PRIVATE /W4 /permissive- /utf-8)
  endif()
  add_test(NAME reference_grid COMMAND reference_grid_tests)
  add_executable(project_workspace_archive_restore_tests tests/project_workspace_archive_restore_tests.cpp)
  target_link_libraries(project_workspace_archive_restore_tests PRIVATE sketch_project_workspace_archive_restore sketch_storage)
  if(MSVC)
    target_compile_options(project_workspace_archive_restore_tests PRIVATE /W4 /permissive- /utf-8)
  endif()
  add_test(NAME project_workspace_archive_restore COMMAND project_workspace_archive_restore_tests)
  add_executable(workspace_save_coordinator_tests tests/workspace_save_coordinator_tests.cpp)
  target_link_libraries(workspace_save_coordinator_tests PRIVATE sketch_workspace_save_coordinator)
  if(MSVC)
    target_compile_options(workspace_save_coordinator_tests PRIVATE /W4 /permissive- /utf-8)
  endif()
  add_test(NAME workspace_save_coordinator COMMAND workspace_save_coordinator_tests)
  add_executable(workspace_save_queue_tests tests/workspace_save_queue_tests.cpp)
  target_link_libraries(workspace_save_queue_tests PRIVATE sketch_workspace_save_queue)
  if(MSVC)
    target_compile_options(workspace_save_queue_tests PRIVATE /W4 /permissive- /utf-8)
  endif()
  add_test(NAME workspace_save_queue COMMAND workspace_save_queue_tests)
  add_executable(workspace_regeneration_queue_tests tests/regeneration_queue_tests.cpp)
  target_link_libraries(workspace_regeneration_queue_tests PRIVATE sketch_workspace_regeneration_queue)
  if(MSVC)
    target_compile_options(workspace_regeneration_queue_tests PRIVATE /W4 /permissive- /utf-8)
  endif()
  add_test(NAME workspace_regeneration_queue COMMAND workspace_regeneration_queue_tests)
  add_executable(performance_telemetry_tests tests/performance_telemetry_tests.cpp)
  target_link_libraries(performance_telemetry_tests PRIVATE sketch_performance_telemetry)
  if(MSVC)
    target_compile_options(performance_telemetry_tests PRIVATE /W4 /permissive- /utf-8)
  endif()
  add_test(NAME performance_telemetry COMMAND performance_telemetry_tests)
  add_executable(workspace_autosave_scheduler_tests tests/workspace_autosave_scheduler_tests.cpp)
  target_link_libraries(workspace_autosave_scheduler_tests PRIVATE sketch_workspace_autosave_scheduler)
  if(MSVC)
    target_compile_options(workspace_autosave_scheduler_tests PRIVATE /W4 /permissive- /utf-8)
  endif()
  add_test(NAME workspace_autosave_scheduler COMMAND workspace_autosave_scheduler_tests)
  add_executable(integration_adapter_tests tests/integration_adapter_tests.cpp)
  target_link_libraries(integration_adapter_tests PRIVATE sketch_integration_adapter)
  if(MSVC)
    target_compile_options(integration_adapter_tests PRIVATE /W4 /permissive- /utf-8)
  endif()
  add_test(NAME integration_adapter COMMAND integration_adapter_tests)
  add_executable(typed_relationships_tests tests/typed_relationships_tests.cpp)
  target_link_libraries(typed_relationships_tests PRIVATE sketch_typed_relationships)
  if(MSVC)
    target_compile_options(typed_relationships_tests PRIVATE /W4 /permissive- /utf-8)
  endif()
  add_test(NAME typed_relationships COMMAND typed_relationships_tests)
  add_executable(room_relationships_tests tests/room_relationships_tests.cpp)
  target_link_libraries(room_relationships_tests PRIVATE sketch_room_relationships)
  if(MSVC)
    target_compile_options(room_relationships_tests PRIVATE /W4 /permissive- /utf-8)
  endif()
  add_test(NAME room_relationships COMMAND room_relationships_tests)
  add_executable(room_relationship_geometry_tests tests/room_relationship_geometry_tests.cpp)
  target_link_libraries(room_relationship_geometry_tests PRIVATE sketch_room_relationships)
  if(MSVC)
    target_compile_options(room_relationship_geometry_tests PRIVATE /W4 /permissive- /utf-8)
  endif()
  add_test(NAME room_relationship_geometry COMMAND room_relationship_geometry_tests)
  add_executable(room_relationship_geometry_commit_tests tests/room_relationship_geometry_commit_tests.cpp)
  target_link_libraries(room_relationship_geometry_commit_tests PRIVATE sketch_room_relationship_geometry_commit)
  if(MSVC)
    target_compile_options(room_relationship_geometry_commit_tests PRIVATE /W4 /permissive- /utf-8)
  endif()
  add_test(NAME room_relationship_geometry_commit COMMAND room_relationship_geometry_commit_tests)
  set_tests_properties(room_relationship_geometry_commit PROPERTIES ENVIRONMENT_MODIFICATION
    "PATH=path_list_prepend:${CMAKE_SOURCE_DIR}/.deps/native/x64-windows/$<IF:$<CONFIG:Debug>,debug/bin,bin>")
  add_executable(model_phases_tests tests/model_phases_tests.cpp)
  target_link_libraries(model_phases_tests PRIVATE sketch_model_phases)
  if(MSVC)
    target_compile_options(model_phases_tests PRIVATE /W4 /permissive- /utf-8)
  endif()
  add_test(NAME model_phases COMMAND model_phases_tests)
  add_executable(import_worker_policy_tests tests/import_worker_policy_tests.cpp)
  target_link_libraries(import_worker_policy_tests PRIVATE sketch_import_worker_policy)
  if(MSVC)
    target_compile_options(import_worker_policy_tests PRIVATE /W4 /permissive- /utf-8)
  endif()
  add_test(NAME import_worker_policy COMMAND import_worker_policy_tests)
  add_executable(windows_import_worker_tests tests/windows_import_worker_tests.cpp)
  target_link_libraries(windows_import_worker_tests PRIVATE sketch_windows_import_worker)
  if(MSVC)
    target_compile_options(windows_import_worker_tests PRIVATE /W4 /permissive- /utf-8)
  endif()
  if(WIN32)
    add_executable(windows_import_worker_probe tests/windows_import_worker_probe.cpp)
    if(MSVC)
      target_compile_options(windows_import_worker_probe PRIVATE /W4 /permissive- /utf-8 /MT)
      set_property(TARGET windows_import_worker_probe PROPERTY MSVC_RUNTIME_LIBRARY "MultiThreaded")
    endif()
    target_link_libraries(windows_import_worker_tests PRIVATE ole32)
    add_test(NAME windows_import_worker COMMAND windows_import_worker_tests "$<TARGET_FILE:windows_import_worker_probe>")
    set_tests_properties(windows_import_worker PROPERTIES SKIP_RETURN_CODE 77)
  else()
    add_test(NAME windows_import_worker COMMAND windows_import_worker_tests)
  endif()
  add_executable(annotation_catalog_tests tests/annotation_catalog_tests.cpp)
  target_link_libraries(annotation_catalog_tests PRIVATE sketch_annotation_catalog)
  if(MSVC)
    target_compile_options(annotation_catalog_tests PRIVATE /W4 /permissive- /utf-8)
  endif()
  add_test(NAME annotation_catalog COMMAND annotation_catalog_tests)
  add_executable(annotation_entity_codec_tests tests/annotation_entity_codec_tests.cpp)
  target_link_libraries(annotation_entity_codec_tests PRIVATE sketch_storage)
  if(MSVC)
    target_compile_options(annotation_entity_codec_tests PRIVATE /W4 /permissive- /utf-8)
  endif()
  add_test(NAME annotation_entity_codec COMMAND annotation_entity_codec_tests)
  add_executable(sheet_view_model_tests tests/sheet_view_model_tests.cpp)
  target_link_libraries(sheet_view_model_tests PRIVATE sketch_sheet_view_model)
  if(MSVC)
    target_compile_options(sheet_view_model_tests PRIVATE /W4 /permissive- /utf-8)
  endif()
  add_test(NAME sheet_view_model COMMAND sheet_view_model_tests)
  add_executable(sheet_view_entity_codec_tests tests/sheet_view_entity_codec_tests.cpp)
  target_link_libraries(sheet_view_entity_codec_tests PRIVATE sketch_storage)
  if(MSVC)
    target_compile_options(sheet_view_entity_codec_tests PRIVATE /W4 /permissive- /utf-8)
  endif()
  add_test(NAME sheet_view_entity_codec COMMAND sheet_view_entity_codec_tests)
  add_executable(georeferencing_entity_codec_tests tests/georeferencing_entity_codec_tests.cpp)
  target_link_libraries(georeferencing_entity_codec_tests PRIVATE sketch_storage)
  if(MSVC)
    target_compile_options(georeferencing_entity_codec_tests PRIVATE /W4 /permissive- /utf-8)
  endif()
  add_test(NAME georeferencing_entity_codec COMMAND georeferencing_entity_codec_tests)
  add_executable(interchange_profile_tests tests/interchange_profile_tests.cpp)
  add_executable(dxf_exchange_tests tests/dxf_exchange_tests.cpp)
  target_link_libraries(dxf_exchange_tests PRIVATE sketch_dxf_exchange)
  add_test(NAME dxf_exchange COMMAND dxf_exchange_tests)
  add_executable(dxf_project_exchange_tests tests/dxf_project_exchange_tests.cpp)
  target_link_libraries(dxf_project_exchange_tests PRIVATE sketch_dxf_project_exchange)
  add_test(NAME dxf_project_exchange COMMAND dxf_project_exchange_tests)
  add_executable(ifc_project_exchange_tests tests/ifc_project_exchange_tests.cpp)
  target_link_libraries(ifc_project_exchange_tests PRIVATE sketch_ifc_project_exchange)
  add_test(NAME ifc_project_exchange COMMAND ifc_project_exchange_tests)
  target_link_libraries(interchange_profile_tests PRIVATE sketch_interchange_profile)
  if(MSVC)
    target_compile_options(interchange_profile_tests PRIVATE /W4 /permissive- /utf-8)
  endif()
  add_test(NAME interchange_profile COMMAND interchange_profile_tests)
  add_executable(field_adapter_contract_tests tests/field_adapter_contract_tests.cpp)
  target_link_libraries(field_adapter_contract_tests PRIVATE sketch_field_adapter_contract)
  if(MSVC)
    target_compile_options(field_adapter_contract_tests PRIVATE /W4 /permissive- /utf-8)
  endif()
  add_test(NAME field_adapter_contract COMMAND field_adapter_contract_tests)
  add_executable(assistance_contract_tests tests/assistance_contract_tests.cpp)
  target_link_libraries(assistance_contract_tests PRIVATE sketch_assistance_contract)
  if(MSVC)
    target_compile_options(assistance_contract_tests PRIVATE /W4 /permissive- /utf-8)
  endif()
  add_test(NAME assistance_contract COMMAND assistance_contract_tests)
  add_executable(assistance_engine_tests tests/assistance_engine_tests.cpp)
  target_link_libraries(assistance_engine_tests PRIVATE sketch_assistance_engine)
  if(MSVC)
    target_compile_options(assistance_engine_tests PRIVATE /W4 /permissive- /utf-8)
  endif()
  add_test(NAME assistance_engine COMMAND assistance_engine_tests)
  add_executable(multipage_document_tests tests/multipage_document_tests.cpp)
  target_link_libraries(multipage_document_tests PRIVATE sketch_multipage_document)
  if(MSVC)
    target_compile_options(multipage_document_tests PRIVATE /W4 /permissive- /utf-8)
  endif()
  add_test(NAME multipage_document COMMAND multipage_document_tests)
  add_executable(assembly_model_tests tests/assembly_model_tests.cpp)
  target_link_libraries(assembly_model_tests PRIVATE sketch_assembly_model)
  if(MSVC)
    target_compile_options(assembly_model_tests PRIVATE /W4 /permissive- /utf-8)
  endif()
  add_test(NAME assembly_model COMMAND assembly_model_tests)
  add_executable(product_scope_tests tests/product_scope_tests.cpp)
  target_link_libraries(product_scope_tests PRIVATE sketch_product_scope)
  if(MSVC)
    target_compile_options(product_scope_tests PRIVATE /W4 /permissive- /utf-8)
  endif()
  add_test(NAME product_scope COMMAND product_scope_tests)
  add_executable(workspace_accessibility_tests tests/workspace_accessibility_tests.cpp)
  target_link_libraries(workspace_accessibility_tests PRIVATE sketch_workspace_accessibility)
  if(MSVC)
    target_compile_options(workspace_accessibility_tests PRIVATE /W4 /permissive- /utf-8)
  endif()
  add_test(NAME workspace_accessibility COMMAND workspace_accessibility_tests)
  add_executable(survey_contract_tests tests/survey_contract_tests.cpp)
  target_link_libraries(survey_contract_tests PRIVATE sketch_survey_contract)
  if(MSVC)
    target_compile_options(survey_contract_tests PRIVATE /W4 /permissive- /utf-8)
  endif()
  add_test(NAME survey_contract COMMAND survey_contract_tests)
  add_executable(georeferencing_contract_tests tests/georeferencing_contract_tests.cpp)
  target_link_libraries(georeferencing_contract_tests PRIVATE sketch_georeferencing_contract)
  if(MSVC)
    target_compile_options(georeferencing_contract_tests PRIVATE /W4 /permissive- /utf-8)
  endif()
  add_test(NAME georeferencing_contract COMMAND georeferencing_contract_tests)
  if(TARGET sketch_georeferencing_runtime)
    add_executable(georeferencing_runtime_tests tests/georeferencing_runtime_tests.cpp)
    target_link_libraries(georeferencing_runtime_tests PRIVATE sketch_georeferencing_runtime)
    if(MSVC)
      target_compile_options(georeferencing_runtime_tests PRIVATE /W4 /permissive- /utf-8)
    endif()
    add_test(NAME georeferencing_runtime COMMAND georeferencing_runtime_tests
      "${CMAKE_SOURCE_DIR}/.deps/native/x64-windows/share")
    set_tests_properties(georeferencing_runtime PROPERTIES ENVIRONMENT_MODIFICATION
      "PATH=path_list_prepend:${CMAKE_SOURCE_DIR}/.deps/native/x64-windows/$<IF:$<CONFIG:Debug>,debug/bin,bin>")
  endif()
  add_executable(architectural_workflow_contract_tests tests/architectural_workflow_contract_tests.cpp)
  target_link_libraries(architectural_workflow_contract_tests PRIVATE sketch_architectural_workflow_contract)
  if(MSVC)
    target_compile_options(architectural_workflow_contract_tests PRIVATE /W4 /permissive- /utf-8)
  endif()
  add_test(NAME architectural_workflow_contract COMMAND architectural_workflow_contract_tests)
  add_executable(architectural_document_adapter_tests tests/architectural_document_adapter_tests.cpp)
  target_link_libraries(architectural_document_adapter_tests PRIVATE sketch_architectural_document_adapter sketch_storage)
  if(MSVC)
    target_compile_options(architectural_document_adapter_tests PRIVATE /W4 /permissive- /utf-8)
  endif()
  add_test(NAME architectural_document_adapter COMMAND architectural_document_adapter_tests)
  set_tests_properties(architectural_document_adapter PROPERTIES ENVIRONMENT_MODIFICATION
    "PATH=path_list_prepend:${CMAKE_SOURCE_DIR}/.deps/native/x64-windows/$<IF:$<CONFIG:Debug>,debug/bin,bin>")
  add_executable(workspace_document_history_tests tests/workspace_document_history_tests.cpp)
  target_link_libraries(workspace_document_history_tests PRIVATE sketch_workspace_document_history)
  add_test(NAME workspace_document_history COMMAND workspace_document_history_tests)
  add_executable(workspace_navigation_tests tests/workspace_navigation_tests.cpp)
  target_link_libraries(workspace_navigation_tests PRIVATE sketch_workspace_navigation)
  add_test(NAME workspace_navigation COMMAND workspace_navigation_tests)
  add_executable(boundary_construction_tests tests/boundary_construction_tests.cpp)
  target_link_libraries(boundary_construction_tests PRIVATE sketch_boundary_authoring)
  if(MSVC)
    target_compile_options(boundary_construction_tests PRIVATE /W4 /permissive- /utf-8)
  endif()
  add_test(NAME boundary_construction COMMAND boundary_construction_tests)
  add_executable(boundary_receipt_tests tests/boundary_receipt_tests.cpp)
  target_link_libraries(boundary_receipt_tests PRIVATE sketch_boundary_receipts)
  add_test(NAME boundary_receipts COMMAND boundary_receipt_tests)
  add_executable(boundary_receipt_integrity_tests tests/boundary_receipt_integrity_tests.cpp)
  target_link_libraries(boundary_receipt_integrity_tests PRIVATE sketch_storage sketch_boundary_receipts sketch_sqlite)
  add_test(NAME boundary_receipt_integrity COMMAND boundary_receipt_integrity_tests)
  add_executable(boundary_commit_tests tests/boundary_commit_tests.cpp)
  target_link_libraries(boundary_commit_tests PRIVATE sketch_boundary_commit)
  add_test(NAME boundary_commit COMMAND boundary_commit_tests)
  if(MSVC)
    foreach(target IN ITEMS boundary_receipt_tests boundary_receipt_integrity_tests boundary_commit_tests)
      target_compile_options(${target} PRIVATE /W4 /permissive- /utf-8)
    endforeach()
  endif()
  add_executable(wall_semantics_tests tests/wall_semantics_tests.cpp)
  target_link_libraries(wall_semantics_tests PRIVATE sketch_wall_semantics)
  add_test(NAME wall_semantics COMMAND wall_semantics_tests)
  add_executable(slab_semantics_tests tests/slab_semantics_tests.cpp)
  target_link_libraries(slab_semantics_tests PRIVATE sketch_slab_semantics)
  add_test(NAME slab_semantics COMMAND slab_semantics_tests)
  add_executable(constraint_entity_tests tests/constraint_entity_tests.cpp)
  target_link_libraries(constraint_entity_tests PRIVATE sketch_document sketch_precision)
  add_test(NAME constraint_entities COMMAND constraint_entity_tests)
  add_executable(constraint_integrity_tests tests/constraint_integrity_tests.cpp)
  target_link_libraries(constraint_integrity_tests PRIVATE sketch_storage)
  add_test(NAME constraint_integrity COMMAND constraint_integrity_tests)
  add_executable(constraint_scaling_tests tests/constraint_scaling_tests.cpp)
  target_link_libraries(constraint_scaling_tests PRIVATE sketch_storage)
  add_test(NAME constraint_scaling COMMAND constraint_scaling_tests)
  add_executable(constraint_unknown_owner_tests tests/constraint_unknown_owner_tests.cpp)
  target_link_libraries(constraint_unknown_owner_tests PRIVATE sketch_storage)
  add_test(NAME constraint_unknown_owners COMMAND constraint_unknown_owner_tests)
  add_executable(project_organization_tests tests/project_organization_tests.cpp)
  target_link_libraries(project_organization_tests PRIVATE sketch_project_organization)
  add_test(NAME project_organization COMMAND project_organization_tests)
  add_executable(project_visibility_tests tests/project_visibility_tests.cpp)
  target_link_libraries(project_visibility_tests PRIVATE sketch_project_visibility)
  add_test(NAME project_visibility COMMAND project_visibility_tests)
  target_link_libraries(document_tests PRIVATE sketch_document)
  add_test(NAME document_commands COMMAND document_tests)
  add_executable(output_fingerprint_tests tests/output_fingerprint_tests.cpp)
  target_link_libraries(output_fingerprint_tests PRIVATE sketch_output_fingerprint)
  add_test(NAME output_fingerprint COMMAND output_fingerprint_tests)
  add_executable(project_store_tests tests/project_store_tests.cpp)
  target_link_libraries(project_store_tests PRIVATE sketch_storage sketch_sqlite)
  add_test(NAME project_storage COMMAND project_store_tests)
  add_executable(project_archive_tests tests/project_archive_tests.cpp)
  target_link_libraries(project_archive_tests PRIVATE sketch_storage sketch_sqlite)
  add_test(NAME project_archive COMMAND project_archive_tests)
  add_executable(project_exchange_tests tests/project_exchange_tests.cpp)
  target_link_libraries(project_exchange_tests PRIVATE sketch_exchange)
  add_test(NAME project_exchange COMMAND project_exchange_tests)
  add_test(NAME project_cli COMMAND "${Python3_EXECUTABLE}" "${CMAKE_SOURCE_DIR}/tests/test_cli.py" "$<TARGET_FILE:property-cli>")

  # A test failure must never require dismissing a CRT or Windows dialog on
  # the developer's desktop. Object linkage retains the initializer even when
  # a test has no explicit reference to the policy helper.
  add_library(sketch_test_process_policy OBJECT tests/support/test_process_policy.cpp)
  target_include_directories(sketch_test_process_policy PRIVATE include)
  get_property(SKETCH_LOCAL_TARGETS DIRECTORY PROPERTY BUILDSYSTEM_TARGETS)
  foreach(target IN LISTS SKETCH_LOCAL_TARGETS)
    if(target MATCHES "(_tests$|^desktop_smoke$|^error_policy_probe$)")
      target_sources(${target} PRIVATE $<TARGET_OBJECTS:sketch_test_process_policy>)
    endif()
  endforeach()
  get_property(SKETCH_LOCAL_TESTS DIRECTORY PROPERTY TESTS)
  set_tests_properties(${SKETCH_LOCAL_TESTS} PROPERTIES TIMEOUT 60)
  if(SKETCH_BUILD_DESKTOP)
    # The comprehensive desktop workflows exercise both workspaces, output,
    # recovery and architectural forms in hidden processes. Keep the ordinary
    # 60-second guard for focused tests, but give these end-to-end scenarios
    # enough time to finish on the supported Windows reference host, including
    # the slower Debug runtime.
    # The full desktop workflow includes native-solid and multi-page output
    # checks. Debug builds are intentionally slower on the reference host;
    # keep the test deterministic instead of letting a valid fixture race a
    # fragile six-minute timeout.
    set_tests_properties(desktop_workflow desktop_workspace_recovery PROPERTIES TIMEOUT 600)
  endif()
endif()
